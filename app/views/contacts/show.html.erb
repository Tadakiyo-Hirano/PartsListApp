<div class="card card-dark">
  <div class="card-header">
    <h3 class="card-title">
      <i class="fas fa-phone-alt nav-icon"></i>
      不具合報告 (送信済 <%= l(@contact.created_at, format: :long) %>)
    </h3>
  </div>
  <div class="card-body">
    <%= Contact.human_attribute_name :name %>： <%= @contact.name %><br>
    <br>
    <%= Contact.human_attribute_name :content %>：
    <p style="white-space: pre-wrap;"><%= @contact.content %></p>
  </div>
</div>
