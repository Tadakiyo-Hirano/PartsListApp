<div id="pageBase">
  <div class="login">
    <h1 class="logo"><img src="/top_logo.png" alt="PARTS LIST"></h1>
  </div>
</div>

<div class="row center">
  <div class="user-password-form col col-md-6 offset-md-3">
    <div class="field-group">
      <h2>パスワード再設定</h2>
      <br/>
      <p>
        以下の欄に登録したメールアドレスを入力してください。<br>
        パスワード再設定について、メールで連絡いたします。
      </p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "users/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "パスワード再設定メールを送る", class: "btn btn-block btn-navy", data: { disable_with: '送信中' } %>
        </div>
      <% end %>

      <div class="shared-links">
        <%= render "users/shared/links" %>
      </div>

    </div>
  </div>
</div>